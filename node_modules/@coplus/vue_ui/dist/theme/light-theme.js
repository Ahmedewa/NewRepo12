const theme = (options = {}) => {
  return {
    purge: [],
    darkMode: false,
    theme: {
      extend: {
        colors: {
          primary: "#2b3a42",
          secondary: "#6c7b88",
        },
      },
    },
    variants: {
      extend: {
        display: ["group-focus"],
      },
    },
    plugins: [],
    ...options,
  };
};

module.exports = { theme };
